# 形象素材说明

形象资源放在项目 **`形象/<形象包名>/`** 下，例如 `形象/银杰动漫形象/`。

在 **`.env`** 里配置：

- `CHARACTER_PACK` — 选择哪个子文件夹  
- 动作触发 — 启动时自动扫描包内图片，见 `形象/README.md`  

详见项目根目录 `.env.example` 与 `README.md`。

本目录 `idle_*.png` 仅在没有可用形象包时作备用走动动画。
